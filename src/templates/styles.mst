{{#isStyledComponent}}
// @flow

import styled from 'styled-components';
import type { ComponentType } from 'react';

import type { StyleProps$Container } from './{{ typesFileName }}';

export const Container: ComponentType<StyleProps$Container> = (() => {
  return styled.div``;
})();
{{/isStyledComponent}}
{{^isStyledComponent}}
.{{ componentName }} {

}
{{/isStyledComponent}}
